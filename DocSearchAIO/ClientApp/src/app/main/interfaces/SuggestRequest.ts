export interface SuggestRequest {
  searchPhrase: string,
  suggestWord: boolean,
  suggestExcel: boolean,
  suggestPowerpoint: boolean,
  suggestPdf: boolean,
  suggestEml: boolean,
  suggestMsg: boolean
}
